const {
      calculateFinancialHealthScore,
      getFinancialHealthStatus,
} = require('./financialAnalytics.service');

module.exports = {
      calculateFinancialHealthScore,
      getFinancialHealthStatus,
};
